{
  "name": "bhby-analytics-dashboard",
  "version": "1.0.0",
  "description": "BHBY Ad-Analytics Dashboard — Netlify serverless functions + Shopify Admin GraphQL",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^17"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
